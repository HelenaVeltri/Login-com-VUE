name: autorelease

on:
  push:
    branches:
      - master
      - teste

jobs:
  release-prod:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar informações do merge
        run: |
          echo "Branch destino: ${{ github.ref_name }}"
          echo "Evento: ${{ github.event_name }}"
          echo "Autor: ${{ github.actor }}"
          echo "Mensagem do commit:"
          echo "MASTERRRRRRRRRRRR"
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Instalar dependências
        run: npm i
      - name: Executar semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx semantic-release
  deu_certo:
    needs: release-prod
    if: ${{needs.release-prod.result == 'success'}}
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar informações do merge
        run: |
          echo "Resultado: ${{ needs.release-prod.result }}" 
          echo "Branch destino: ${{ github.ref_name }}"
          echo "Evento: ${{ github.event_name }}"
          echo "Autor: ${{ github.actor }}"
          echo "Mensagem do commit:"
          echo "MASTERRRRRRRRRRRR"
          
  release-test:
    if: github.ref == 'refs/heads/teste'
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar informações do merge
        run: |
          echo "Branch destino: ${{ github.ref_name }}"
          echo "Evento: ${{ github.event_name }}"
          echo "Autor: ${{ github.actor }}"
          echo "Mensagem do commit:"
          echo "Secretgithub token: ${{ secrets.GITHUB_TOKEN}}"
          echo "Bananilson farofa é legal pra caramba"
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Instalar dependências
        run: npm i
      - name: Executar semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx semantic-release

  deu_certo:
    needs: release-test
    if: ${{needs.release-test.result == 'success'}}
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar informações do merge
        run: |
          echo "Resultado: ${{ needs.release-test.result }}" 
          echo "Branch destino: ${{ github.ref_name }}"
          echo "Evento: ${{ github.event_name }}"
          echo "Autor: ${{ github.actor }}"
          echo "Mensagem do commit:"
          echo "MASTERRRRRRRRRRRR"
